[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
dockerfilePath = "Dockerfile"
startCommand = "uvicorn pipecatFloww_livekit_twilio:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
